 try {
                    LOGGER.info("Ship " + model + " went to the port");
                    try {
        TimeUnit.SECONDS.sleep(5);
    } catch (InterruptedException e) {
        e.printStackTrace();
    }
                    port.getLock().lock();
                    try {
        if (serviceType.toString().equals("LOAD")) {
            LOGGER.info("Ship " + model + " is loading");
            load();
        } else if (serviceType.toString().equals("DELIVERY")) {
            LOGGER.info("Ship " + model + " is delivering");
            deliver();
        } else if (serviceType.toString().equals("MULTISERVICE"))
            LOGGER.info("Ship " + model + " is delivering and loading");
        deliver();
        load();
    } finally {
        port.getLock().unlock();
        try {
            TimeUnit.SECONDS.sleep(5);
        } catch (InterruptedException e) {
            e.printStackTrace();
        }
        LOGGER.info("Ship " + model + " finished");
        running = true;
    }
} finally {
        LOGGER.info("Ship " + model + " left the port");
        port.getLock().unlock();
        }
        } {